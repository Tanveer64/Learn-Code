package fifteen.features;

//import jdk.incubator.foreign.CLinker;
import jdk.incubator.foreign.MemoryAddress;
import jdk.incubator.foreign.MemorySegment;

/*
This feature allows the loading of native libraries and C header files. This feature allows developers to access
native libraries and headers directly from java code. Below is an example of the above feature:
*/
public class ForiegnLinkerAPI {
//    public static void main(String[] args){
//        CLinker cl = CLinker.getInstance();
////        try (MemorySegment segment = cl.allocate(16, true)) {
////            MemoryAddress address = segment.baseAddress();
////            cl.putInt(address, 42);
////            int value = cl.getInt(address);
////            System.out.println(value);
////        }
//    }
}